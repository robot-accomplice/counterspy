{
  "flows": [
    {
      "id": "scan-pipeline",
      "name": "Scan pipeline (collect → score → interpret → report)",
      "status": "implemented",
      "summary": "The read-only scan: fan out four collectors, score+correlate, synthesize recommendations, render a ranked report. Never mutates.",
      "trigger": "operator runs `sudo counterspy scan`",
      "actors": ["operator"],
      "steps": [
        {"id": "s1", "from": "operator", "to": "counterspy-cli", "action": "runScan", "kind": "start", "summary": "Invoke scan (optionally --json / --interactive).", "data": []},
        {"id": "s2", "from": "counterspy-cli", "to": "mod-collect", "action": "collectAll", "kind": "process", "summary": "Fan out collectors; a failing collector is reported as a gap, never silence.", "data": []},
        {"id": "s3", "from": "mod-collect", "to": "macos-tools", "action": "execNativeTools", "kind": "process", "summary": "Shell out to launchctl/plutil/codesign/spctl/ps/lsof/sqlite3.", "data": ["process-list", "code-signatures", "tcc-grants"]},
        {"id": "s4", "from": "mod-collect", "to": "mod-score", "action": "Score", "kind": "process", "summary": "Group evidence by subject, sum weights, correlation multiplier, suppress allowlisted, fire tripwires.", "data": ["scan-findings"]},
        {"id": "s5", "from": "mod-score", "to": "mod-interpret", "action": "Assess", "kind": "process", "summary": "Rule-based verdict + category + recommendation per finding.", "data": ["scan-findings"]},
        {"id": "s6", "from": "mod-interpret", "to": "mod-report", "action": "Render", "kind": "process", "summary": "Executive summary + ranked actionable findings; Monitor tier counted, not dumped.", "data": ["scan-findings"]},
        {"id": "s7", "from": "mod-report", "to": "operator", "action": "printReport", "kind": "return", "summary": "Ranked report (or --json).", "data": ["scan-findings"]}
      ],
      "guarantees": ["Read-only", "Missing signals reported as gaps (fail loud)", "Deterministic verdicts"],
      "failureBehavior": ["A collector that cannot read its source prints a gap line; the scan still completes with the signals it has"],
      "observability": ["Ranked report", "--json", "gap lines"],
      "verification": ["go test ./...", "manual `counterspy scan` on a real Mac"],
      "knownGaps": ["False-positive volume on developer machines (see risk-false-positive-volume)"]
    },
    {
      "id": "quarantine",
      "name": "Consented quarantine (disable → move → manifest)",
      "status": "implemented",
      "summary": "The only mutating flow: on per-item approval, disable the launch item and MOVE its artifacts into a reversible quarantine, recording a manifest.",
      "trigger": "operator answers y to a finding in `scan --interactive`",
      "actors": ["operator"],
      "steps": [
        {"id": "q1", "from": "operator", "to": "counterspy-cli", "action": "approveQuarantine", "kind": "start", "summary": "Per-item consent (y/N/q).", "data": ["scan-findings"]},
        {"id": "q2", "from": "counterspy-cli", "to": "mod-act", "action": "Quarantine", "kind": "process", "summary": "Refuse allowlisted/protected paths; bootout; move artifacts into the quarantine mirror.", "data": ["quarantine-manifest"]},
        {"id": "q3", "from": "mod-act", "to": "operator", "action": "confirmReversible", "kind": "return", "summary": "Quarantined to ~/CounterSpyQuarantine/<ts>; reversible via `restore`.", "data": ["quarantine-manifest"]}
      ],
      "guarantees": ["Never deletes — moves only", "Reversible (restore round-trips byte-identically)", "Partial failures still write a manifest so nothing is orphaned"],
      "failureBehavior": ["Refuses Apple-signed/allowlisted and protected system paths; on a mid-way move failure, stops and records the partial manifest"],
      "observability": ["manifest.json (undo + RCA trail)"],
      "verification": ["go test ./internal/act/"],
      "knownGaps": ["A live process with no on-disk artifact cannot be quarantined (surfaced for manual action)"]
    }
  ]
}
